import { VaultWizard } from "@/components/vault/VaultWizard";

export const metadata = { title: "Create vault — Vault" };

export default function NewVaultPage() {
  return (
    <div>
      <VaultWizard />
    </div>
  );
}
